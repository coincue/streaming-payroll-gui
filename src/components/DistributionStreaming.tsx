// This component is no longer used - streaming payments have been removed
// Kept as a stub to avoid breaking imports if needed in the future

export default function DistributionStreaming() {
  return null
}
